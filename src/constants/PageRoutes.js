const home = "/";

const CommonRoutes = {
    MAIN: home,
}

const login = "/login"

const AuthRoutes = {
    LOGIN: login,
}

export const PageRoutes = {
    COMMON: CommonRoutes,
    AUTH: AuthRoutes,
};